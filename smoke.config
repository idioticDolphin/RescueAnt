# Bounded validation run: real pages, own database and page store, no
# search discovery, stops after two rounds.
include "bot.config";
database = "smoke.db";
page_store_path = "store_smoke";
discover_urls = False;
max_rounds = 2;
