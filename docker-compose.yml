services:
  searxng:
    image: searxng/searxng:latest
    container_name: rescueant-searxng
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - ./searxng:/etc/searxng
    restart: unless-stopped
